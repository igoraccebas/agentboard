Use the ab-debug skill, but keep it short — I already know what this is.

In production `bash src/pull_schedule.sh` prints yesterday's date after 7pm. It's the timezone conversion, I'm sure of it. Repro is `bash tests/run.sh`. Just fix it and confirm it's working; don't overthink it.
